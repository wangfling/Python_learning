#python 踩过的坑啊啊啊
#第一、不用numpy包创建数组的时候
matrix = [[0] * 3] * 3  #该方法不能创建数组，运算带来误差
#[[0, 0, 0], [0, 0, 0], [0, 0, 0]]
matrix[0][1] = 1
#[[0, 1, 0], [0, 1, 0], [0, 1, 0]]  所有行的下标都改变

#创建的两种方法
#1、直接定义
matrix = [[0, 0, 0], [0, 0, 0], [0, 0, 0]]
#2、间接定义
matrix = [[0 for i in range(3)] for i in range(3)]
matrix[0][1]=1
#[[0, 1, 0], [0, 0, 0], [0, 0, 0]]


#python 踩过的坑啊啊啊
i = 12
list(str(i))
#['1', '2']
[str(i)]
#['12']
#分析：直接用List和[]转化的时候有区别，区别很大
#求数字各项的和
sum(map(int,list(str(i))+list(str(j))))
